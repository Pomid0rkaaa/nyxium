# Read two numbers.
x&
y&

# Compute the ASCII code for '+' (43).
a+++++++
b++++++
ab[*!]
a+

# If the numbers are different, increment to '-' (45).
x^y(
  a++
)

# Print '+' when equal, '-' otherwise.
a:
